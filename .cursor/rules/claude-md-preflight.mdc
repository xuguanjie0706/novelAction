---
description: 改代码前必读 CLAUDE.md（项目记忆唯一来源）
alwaysApply: true
---

# CLAUDE.md 强制预检

在**任何**创建/修改/删除代码或配置之前：

1. 使用 **Read** 工具读取仓库根目录 `CLAUDE.md`（本会话已完整读过且用户未要求刷新时可跳过）。
2. 动手前至少对照这些章节：
   - 「代码结构红线」「上帝文件登记册」
   - 「设定生成架构原则」「API 路由约定」
   - 「开发建议（给未来的 Claude）」
3. 若目标路径在登记册中：**禁止**在原上帝文件堆功能；按拆分蓝图落新文件；净增 ≥50 行须同步「治旧」删除。
4. 架构/待办/登记册变更**只写 `CLAUDE.md`**，不抄进 `AGENTS.md`。
5. 回复用户时用一句话说明已对照的章节（不必贴全文）。
