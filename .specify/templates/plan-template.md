# 实现计划：[FEATURE]

**分支**：`[###-feature-name]` | **日期**：[DATE] | **规格**：[link]  
**输入**：来自 `/specs/[###-feature-name]/spec.md` 的功能规格

**说明**：本文件由 `/speckit.plan` 命令填写。执行流程说明见 `.specify/templates/plan-template.md`。

## 摘要

[从功能规格提取：主要需求 + 调研后的技术思路]

## 技术上下文

<!--
  必填：将本节替换为项目技术细节；下列结构为建议骨架。
-->

**语言/版本**：[例如 Python 3.11，或写「待澄清」]  
**主要依赖**：[例如 FastAPI，或「待澄清」]  
**存储**：[若适用：PostgreSQL、文件等；否则 N/A]  
**测试**：[例如 pytest，或「待澄清」]  
**目标平台**：[例如 Linux 服务器、iOS 15+，或「待澄清」]  
**项目类型**：[例如 库 / CLI / Web 服务 / 移动应用，或「待澄清」]  
**性能目标**：[领域相关，或「待澄清」]  
**约束**：[例如 p95 延迟、内存上限、离线能力，或「待澄清」]  
**规模/范围**：[例如用户数、代码量、页面数，或「待澄清」]

## 宪章检查

*门禁：阶段 0 调研前必须通过；阶段 1 设计后须复核。*

对照 `.specify/memory/constitution.md` 核查：

- **I. 控制面主轴**：本功能是否避免在无迁移说明下增加第二条用户可见对话/API 主线？  
- **II. 契约先行**：跨服务执行处是否已规定幂等、取消、超时与循环上限？  
- **III. 切片交付**：若双写或开关，是否写明读路径权威与收敛预期？  
- **IV. 安全基线**：用户范围、密钥、委托信任边界是否覆盖？  
- **V. 可测交付**：规格中场景与成功标准是否存在且可度量？
- **VI. 完整交付闭包**：合并默认分支前是否具备可复现验收与发布说明（含已知限制），避免仅有计划无闭环？

## 项目结构

### 文档（本特性）

```text
specs/[###-feature]/
├── plan.md              # 本文件（/speckit.plan 输出）
├── research.md          # 阶段 0 输出（/speckit.plan）
├── data-model.md        # 阶段 1 输出（/speckit.plan）
├── quickstart.md        # 阶段 1 输出（/speckit.plan）
├── contracts/           # 阶段 1 输出（/speckit.plan）
└── tasks.md             # 阶段 2 输出（/speckit.tasks，非 plan 创建）
```

### 源码（仓库根目录）

<!--
  必填：将下方占位目录树替换为实际布局；删除未用选项；交付物中不得保留「Option」字样。
-->

```text
# [未用则删] 选项 1：单项目（默认）
src/
├── models/
├── services/
├── cli/
└── lib/

tests/
├── contract/
├── integration/
└── unit/

# [未用则删] 选项 2：Web（检测到 frontend + backend）
backend/
├── src/
│   ├── models/
│   ├── services/
│   └── api/
└── tests/

frontend/
├── src/
│   ├── components/
│   ├── pages/
│   └── services/
└── tests/

# [未用则删] 选项 3：移动 + API（检测到 iOS/Android）
api/
└── [与上列 backend 同形]

ios/ 或 android/
└── [平台相关：功能模块、界面流、平台测试]
```

**结构决策**：[写明所选结构并引用上表真实路径]

## 复杂度追踪

> **仅在宪章检查存在必须解释的违规时填写**

| 违规项 | 为何需要 | 更简单方案不足的原因 |
|--------|----------|----------------------|
| [例如第四个项目] | [当前需求] | [为何三个项目不够] |
| [例如仓储模式] | [具体问题] | [为何直连数据库不够] |
