# 项目编辑级 Review — 2026-05-05

> 视角：30 年总编辑（先看作品，再看系统）
> 范围：apps/backend/app/**（services/routers/models 全量阅读），CLAUDE.md / AGENTS.md
> 阅读重心：bootstrap 12 步、draft/quality/debrief 链路、数据模型外键完整性、prompt 注入实情

---

## 一、一句话结论

**这个系统目前是一台"会自动填表的设定生成器"，不是一台"会做编辑工作的写作系统"。**
框架（bootstrap、伏笔台账、章节索引、跨章一致性）已经搭得不错；但**真正决定网文生死的几道工序——题材分流、分镜调度、人物语风指纹、读者期待管理、复盘回写到下一章——要么缺失，要么只走到一半。** 这就是为什么"AI 味重、节奏平、人物漂"会反复出现。下面按问题严重程度拆解。

---

## 二、内容侧的七个根因（编辑视角）

### 1. 没有"流派分流"，所有项目共用一套模板

**现象**：bootstrap 24 张设定卡蓝图、8 人定额角色（1 主 + 3 核 + 2 反 + 2 师）、6 种 phase（opening/rising/turning/dark_hour/climax/ending）、prompt 模板，**对玄幻 / 悬疑 / 都市 / 科幻 / 言情一视同仁**。

**编辑视角**：流派是网文的第一性。
- 玄幻开局要"金手指 + 打脸 + 第一次升级"；
- 悬疑开局要"反常事件 + 嫌疑铺设 + 信息差"；
- 都市开局要"身份反差 + 装逼前奏 + 利益冲突"；
- 言情开局要"怦然心动 + 误会埋点 + 第一次靠近"。
"opening 期、爽点 3 章一次、字数 ~2200"这种通用约束，到任何流派都对，但任何流派都做不出最优。

**代码证据**：
- `generation_service.py` 的 `GEMINI_SETTING_BLUEPRINTS`（24 张固定蓝图）对所有项目生效；
- `_single_shot_prompt` 以及各步 `_gen_*` 都没有按 `genre` 分支；
- `phase` 文案在 `ai_service.py:1215-1254` 是写死的 6 段，任何流派一个样。

**改法（P0）**：
- 把"流派"升级为一等公民：`Project.genre_family ∈ {xuanhuan, xianxia, urban, mystery, romance, scifi, kung_fu, ...}`；
- 每个流派维护一份 `genre_kit`：开局必备节拍、爽点类型、配角配额、设定卡蓝图、对白语风、章末钩子谱；
- bootstrap 第 0 步（positioning）后立刻分流，后面 11 步全部读 genre_kit 做模板替换；
- prompt 层引入"反例约束"——告诉模型这个流派**不应该**出现什么（悬疑别一上来打脸，玄幻别一上来失忆）。

---

### 2. 写章节是"一把梭"，缺少"章纲 → 分场 → 正文"三层调度

**现象**：`draft_assist_stream` 直接从 `OutlineNode`（核心事件、章末方向）跳到 ~2200 字正文，没有中间的 beat / 分场环节。

**编辑视角**：这是 AI 味最大的工程性根因。真正的写作流程是：
1. **章纲**（300 字）：本章要解决的剧情债 + 钩子兑现承诺 + 章末新债务
2. **分场**（每场 50-150 字）：场景 = 时间 + 地点 + POV + 在场角色 + 目标 + 冲突 + 转折 + 出场钩子
3. **正文**：按分场展开，每场字数预算，写完 self-critic 一次

跳过 2 直接写 3，模型只能"展开式"输出——加几句环境，加几句对白，推一点剧情，再加几句心理活动——**节奏是平的**，因为它在写句子的时候不知道这一段在结构里的位置。

**代码证据**：`ai_service.py:1403-1412` 的 prompt 拼装直接给"标题 / 钩子 / 核心事件 / 人物变化 / 章末方向"五行就开写。

**改法（P0）**：
- 新增中间 endpoint `/draft/scene-plan`：输入 OutlineNode + 上下文，输出 `scenes: [{order, location, time, pov, characters_on_stage, goal, conflict, turn, hook, word_budget, sensory_focus}]`；
- 正文生成改为"逐场展开 + 全章 stitch"两阶段；
- `Scene` 单独建表，作为 OutlineNode 的子表（见数据模型一节）；
- 这一改动会让"AI 味"显著降一档，比调 temperature 有用 10 倍。

---

### 3. 没有"反 AI 修辞清单"和"句式硬约束"

**现象**：v3 加了 task profiles，draft.chapter 用 T=0.9 + frequency/presence_penalty——这是治标。AI 味的真正来源是**一组高频套话**：
- "心中暗道 / 暗自思忖 / 不动声色"
- "嘴角微微勾起 / 眼底闪过一丝 X"
- "空气仿佛凝固 / 时间仿佛静止"
- "深吸一口气 / 缓缓睁开眼"
- "他知道，从这一刻起……"
- 全知第三人称 + 大段排比 + 形容词堆砌

**编辑视角**：这些不是"温度高就能避免"的，模型训练数据决定了它会反复掉进。真编辑会拿红笔划掉，然后让作者**改掉这一段**。

**改法（P1）**：
- 维护一份项目级 / 流派级 `cliché_blacklist`（200-500 条）；
- draft prompt 末尾强约束："以下表达本章不得出现：……"；
- 写完后增加 `self_critic` 阶段：用 quality.* 档位（T=0.2）扫一遍正文，命中黑名单则返回需重写句子的位置 + 原因，再用 draft 档位重写这几句；
- 这一步的成本是多 1 次 LLM 调用，但收益是质感跨级。

---

### 4. 人物会"漂"——缺 stylekit、缺 POV、缺戏份预算

**现象**：
- Character 表有 `speech_style`（自由文本）、`alias`、`appearance`，但**没有语料库**——AI 不知道 A 角色具体怎么"咬字、用词、句式";
- Chapter / OutlineNode **无 POV 字段**，全知第三人称是默认；
- 多人物章节没有"戏份预算"——AI 凭直觉给镜头，3-5 章后核心配角就开始失语；
- 复盘的 `character_updates` 只更新 `current_realm/location/status`，**不更新语风、不更新口头禅、不沉淀新出现的语言习惯**。

**代码证据**：
- `models/character.py` 中 `speech_style` 是 `Text`，无样本字段；
- `routers/ai/draft_routes.py` 上下文构建未注入"本章主要 POV 人物的语风样本"；
- `services/character_gap.py` 的"角色缺口检测"做的是结构性查漏，不做语风指纹。

**改法（P1）**：
- `Character.speech_kit` JSON 字段：`{signature_words, sentence_length_pref, taboo_words, sample_dialogues:[5-8 句], inner_monologue_style}`；bootstrap 时 AI 一次性生成；写章后由复盘自动追加新样本；
- `OutlineNode.scene_pov` 或 `Scene.pov`（见 §5），强制视点；
- `OutlineNode.character_screen_time` JSON：`{char_id: word_budget_pct}`，写章 prompt 注入；
- draft prompt 注入本章在场角色的 `speech_kit` 节选 + 一条"严禁让 A 说出 B 风格的台词"。

---

### 5. 复盘没有闭环——auto-debrief 只生成、不驱动下一章

**现象**：`debrief_routes.py:474` 的 `auto-debrief` 只返回 JSON 给前端"预填表单"，需要用户手动确认才落库；落库之后，**也只更新当前章相关的 Character / StoryLine / Memory，没有反向修改后续 OutlineNode**。

**编辑视角**：编辑做复盘永远是"为了下一稿"。本章节奏拖了 → 下章前两场要快；本章 A 角色没出场 → 下章要补戏；本章埋的伏笔太满 → 下章必须收一个。这些都是**对未来 OutlineNode 的差量修改**。

**改法（P0）**：
- 复盘 schema 增加 `next_chapter_directives: [{outline_node_id, patch:{...}}]`；
- 落库时同步 patch 到目标 OutlineNode 的 `extra.directives_from_prev`；
- 下章 draft prompt 高优先级注入这些 directives；
- auto-debrief 默认走"先存草稿、用户审一眼一键 apply"，而不是"必须填一遍表"。

---

### 6. 伏笔是"账本"，不是"叙事节拍"

**现象**：`Foreshadow` 有 `status ∈ {open, resolved, dropped}`、`priority`，但缺：
- `planned_resolve_chapter`（约定回收章）；
- `type ∈ {hook, reversal, prop, identity, prophecy}`（伏笔类型）；
- `min_distance / max_distance`（埋后多少章内必须回收，否则告警）；
- `paid_off_quality`（回收质量评分，0-5，由质检填）；
- `audience_aware`（埋的时候读者能不能感知到——影响后续兑现的"惊喜值"）。

**编辑视角**：网文里"伏笔超期未收"是常见死亡原因。一本书埋下 80 个伏笔，最后只回收 30 个，剩下的 50 个变成"作者忘了写"。台账要带预算 + 告警。

**改法（P1）**：
- Foreshadow 加上述字段；
- 提供 `/foreshadow/audit` 接口：列出超期伏笔、低质量回收、未感知伏笔；
- bootstrap 第 9 步生成 volumes 时，强制每卷分配"本卷应回收伏笔预算 + 本卷应埋设伏笔预算"。

---

### 7. 缺"读者期待管理"——网文最重要的看不见的工序

**现象**：项目里没有任何表 / 字段记录"对读者公开承诺过什么"。

**编辑视角**：网文的成败 70% 取决于"答应过什么 + 什么时候兑现"。
- 章末预告："下一章，林凡将面对天劫"——这是承诺，必须 1 章内兑现；
- 卷末预告："下一卷，他们将进入魔渊"——必须本卷开头到达；
- 章评区高频提及的"什么时候打 X"——必须在 5-10 章内安排；
- 主角名字 / 技能名字本身就是承诺（叫"剑神"必须有用剑封神的桥段）。

**改法（P2 但回报巨大）**：
- 新增表 `ReaderPromise`：`{promise_text, source_chapter, type ∈ {章末预告, 卷末预告, 名字暗示, 章评共识, 主角自白}, expected_chapter_window, status ∈ {open, fulfilled, broken}, fulfilled_chapter_id}`；
- 写章 prompt 自动注入"本章必须兑现的承诺 / 本章可以新建的承诺额度"；
- 复盘自动检测新承诺、回收旧承诺；
- 这是网文系统从"AI 写文"升到"AI 当编辑"的关键。

---

## 三、流程层断裂（bootstrap → 写章节）

### 8. positioning 注入位置错了

`Project.extra.positioning`（受众、爽点、打脸节奏、卖点）只在 `draft_assist_stream` 写章节时被读到。**bootstrap Step 9（生成卷骨架）和 Step 8（生成设定卡）不读它。** 结果：卷级冲突 / 设定卡和读者画像不耦合，章节阶段才补救。

**改法（P0，改动小）**：在 `_gen_volumes` / `_gen_settings` / `_gen_storylines` / `_gen_characters` 的 ctx 里直接注入 positioning JSON。一个 ctx 字段的事，立竿见影。

### 9. 8 个 bootstrap 步骤没传 task 名

子 agent 报告显示，`settings` / `storylines` 等步骤没传 `task=` 走网关默认采样，等于把 v3 的精细化档位漏出去了。补齐即可（改动 < 30 行）。

### 10. 跨步骤名字 → ID 映射易断裂

`mastered_by_character_ids` 用 char_name → char_id 字典匹配，繁简、空格、谐音、别名都会导致丢失。改法：
- bootstrap 写人物时同时生成稳定的 `slug`（拼音 + 序号）；
- 后续步骤用 slug 而非汉字名匹配；
- 缺失时不要静默跳过，至少 log warn 让用户感知。

### 11. JSON 解析对类型容错不够

`_parse_json` 只去 markdown fence、think 标签、首字符截取。LLM 偶尔会返回 `"age": "二十岁"` 而 schema 期望 int，目前会崩。建议在 schema 解析层加一个 `_coerce_to_int / _coerce_to_str_list` 的统一兜底，不要每个字段单独写 try/except。

---

## 四、数据模型层（编辑视角 + 工程视角双角度）

### 12. JSON 字段过载，关系语义全藏在字典里

- `OutlineNode.involved_character_ids`（JSON）—— 反查"某角色在哪些章节出场"必须扫全表 + 解 JSON；
- `Skill.mastered_by_character_ids`（JSON）—— 同上；
- `Faction.key_members`（JSON）—— 势力成员查询要扫描；
- `Item.ownership_history`（JSON）—— 没法做"谁在哪一章接手了 X"的索引。

**改法（P1）**：上述全部建中间表（`outline_node_character`、`skill_master`、`faction_member`、`item_ownership_event`），保留 JSON 作为 denormalized cache 即可。这一改动的回报是"编辑级别的全局检索"——你能瞬间问出"林凡在 47 章出现的密度是不是断崖下降"。

### 13. 缺 `Scene` 表（已在 §2 提过，这里给完整 schema 建议）

```
Scene:
  id, project_id, chapter_id, outline_node_id (nullable, 本章不一定每场都对齐 outline)
  order, title
  time, location_id, story_day  (统一时间线锚点)
  pov_character_id              (强制视点)
  characters_on_stage [FK 多对多]
  goal, conflict, turn, hook
  word_budget, actual_word_count
  pacing ∈ {fast, mid, slow}
  sensory_focus ∈ {sight, sound, smell, taste, touch, mixed}
  cliffhanger_strength 0-5      (场末张力，质检填)
```

### 14. 缺 `Location` 表

地理位置目前在 chapter content 里全是自由文本。编辑级一致性要求"角色在 17 章在京城，18 章直接在东海"是要被告警的——目前完全做不到。

### 15. 缺 `CharacterMomentum` 表

每章每角色出镜字数 / 关键动作数。可以从 chapter_content 后处理生成（写完一章后跑一遍）。用途：检测"二号女主已经 12 章没戏份了"。

### 16. dead columns 列表

明确死掉或几乎死掉的字段（建议或者用上、或者删掉）：
- `Character.trauma`、`Character.fear` —— 在 prompt 里几乎不读；
- `Character.arc_stages` —— 设计上要驱动人物弧线，实际没在 draft prompt 注入；
- `OutlineNode.reader_hook_score` —— 字段存在但没有生成路径；
- 整张 `WorldSetting` 表 —— 在新结构（PowerSystem / Faction / Skill / Item）出来之后变成"纯叙事文字卡"，但 draft prompt 对它的引用很弱；
- `Faction.history`、`Faction.secrets` —— 写完不再用。

### 17. 状态机散乱

- `Chapter.status ∈ {draft, writing, done, reviewed}`，`OutlineNode` 没有 status；
- `Foreshadow.status` 与 `planned_action` 概念重叠；
- `MemoryChunk` 无 status，无法标记"过时 / 修正"。
建议引入一个全局 `lifecycle.py` 集中定义所有枚举和合法转换。

---

## 五、AI 生成质量层（不止 temperature 的事）

### 18. 质检维度太"通用文学" 不"网文"

当前 6 维：plot / character / setting_consistency / pacing / hooks / outline_alignment。编辑视角缺：
- 黄金三章评估（前 3 章追读引爆度）；
- 章末钩子等级（1-5 星，1 星 = 软结，5 星 = 强反转 / 重磅信息）；
- 信息密度（每 500 字新信息条数）；
- 爽点节奏（打脸、装逼、反转、装备 / 升级、虐渣、暧昧）频率；
- 扁平度（人物只是行为傀儡 vs 有内心冲突）；
- 对话辨识度（盖掉名字能否区分 A 和 B）；
- POV 漂移检测；
- 套话浓度（命中黑名单密度）。

**改法（P0）**：扩到 12-14 维，每维给 1-5 评分 + 一段诊断 + 一条具体改写建议（quality 档位 T=0.2-0.3 完全够用）。

### 19. coherence（跨章一致性）查不到节奏 / 人物行为细节

目前查 title_match / continuity / OOC / timeline_break。还应该查：
- 节奏跳变（A 章慢、B 章超快、C 章又慢——读者会跳订）；
- 信息来源溯源（A 角色突然知道 X，前文没他获知 X 的桥段）；
- 对话风格一致性（同一角色在 5 章和 12 章的语气差太多）；
- 战力漂（金丹期击败元婴期没合理代价）。

### 20. 没有"读者模拟器"

这是网文系统的差异化大杀器。让 AI 扮演 3-5 类读者画像（追新型、考据党、CP 党、爽文党、剧情党），各自对刚写完的章节给一段"章评"+ 一个"会不会追下一章"的判断。比单纯的 quality_check 反映更多信号。
**ROI 极高，工程成本极低。**

---

## 六、优先级路线图（给一个 4 周节奏）

> 按"投入 / 产出 / 风险"排序。每一项后面带预估改动量。

### Week 1 — 救命级（不动它系统就废一半）

1. **流派分流** — 在 bootstrap 第 0 步后引入 `genre_kit`，至少分 4 类（玄幻 / 都市 / 悬疑 / 言情），后续 11 步全部按 kit 分支。【中等改动，2-3 天】
2. **positioning 全面注入** — `_gen_volumes / _gen_settings / _gen_storylines / _gen_characters` ctx 里加 positioning JSON。【小改动，半天】
3. **8 个 bootstrap 步骤补 task=** — v3 档位发挥作用。【极小，2 小时】
4. **JSON 解析的类型 coerce 兜底** — 集中到 `_parse_json` 后置一个 schema-aware coercer。【中等，1 天】
5. **复盘闭环** — auto-debrief 加 `next_chapter_directives`，落库时 patch 到下一章 OutlineNode；下章 draft prompt 高优先级注入。【中等，2 天】

### Week 2 — 提质级（解 AI 味）

6. **章纲 → 分场 → 正文 三层调度** — 新建 `Scene` 表 + `/draft/scene-plan` endpoint + 正文逐场生成。【大改动，4-5 天】
7. **反 AI 修辞黑名单 + self-critic 重写** — 黑名单维护成 yaml，draft 后增加一次扫描重写。【中等，2 天】
8. **质检维度扩到 12-14 维** — 加黄金三章 / 信息密度 / 爽点节奏 / 扁平度 / 对话辨识度 / POV 漂移 / 套话浓度。【中等，2 天】
9. **Character.speech_kit** — bootstrap 一次性生成，复盘累积样本。【小，1 天】
10. **POV 字段** — Scene.pov_character_id 强制注入。【极小，半天，但收益巨大】

### Week 3 — 全局智能级

11. **伏笔台账升级** — 加 type / planned_resolve_chapter / min_max_distance / paid_off_quality / audience_aware；提供 audit 接口。【中等，2 天】
12. **关系中间表** — outline_node_character / skill_master / faction_member / item_ownership_event；保留 JSON 作 cache。【中等，2 天】
13. **CharacterMomentum** + **Location 表** + **戏份预算注入**。【中等，2 天】
14. **读者模拟器** — 3-5 个画像，章末模拟章评。【小，1 天】

### Week 4 — 编辑级

15. **ReaderPromise 表** + 章末预告 / 卷末预告 / 名字暗示自动登记 + 兑现追踪。【中等，3 天】
16. **节奏仪表盘** — 给 client 端一个全局视图：爽点频率 / 钩子密度 / 各角色出镜曲线 / 伏笔健康度。【中等，2-3 天】
17. **dead column 清理 + 状态机集中化**。【小，1 天】

---

## 七、给"AI 全流程"的一句心得

**真正卡 AI 全流程的不是模型能力，是"工序"。**
现在的系统欠的是"工序"——分镜、语风、视点、戏份预算、承诺管理、流派指纹、复盘闭环——这些是编辑岗位过去 100 年积累下来的工业化分工。一旦把这些工序"写进系统"（每一道工序对应一张表 / 一个 endpoint / 一段 prompt 注入），AI 全流程会从"看着挺像、读着尴尬"跨到"读者愿意追到完本"。

模型层的事（调温度、换 Gemini）只能拿到 +10%；工序层的事，能拿到 +200%。

---

*Review by 总编辑视角 — 2026-05-05*
