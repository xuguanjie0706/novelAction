立即修复：auto_extract_debrief的max_tokens从1500改为4096，这是当前最严重的bug，复盘几乎一定会被截断。
写章节与复盘必须自动串联：章节生成完成后，后端自动触发复盘，不等用户操作。人物状态、故事线状态、连续性账本立即入库。
实现pgvector语义记忆检索：不做这个，500章之后的长篇连贯性是无法保证的。这是整个系统能不能真正用于长篇创作的前提。
Bootstrap补全章节计划生成：Bootstrap完成后，不只是卷骨架，要生成全部章节的五要素计划。用户拿到的应该是一本书的完整骨架，而不只是目录标题。
Skill/Item的mastered_by关联character UUID：当前是已知断链，需要在Bootstrap后做一次关联修正（通过名字匹配+AI辅助确认的方式）。
增加一致性自检步骤：Bootstrap结束后，以及每10章写完后，自动触发一次全局一致性检查——AI读取所有人物状态、境界、位置、伏笔状态，输出矛盾报告。
商业立项环节的加入：在Bootstrap之前增加一步premise_analysis，让AI分