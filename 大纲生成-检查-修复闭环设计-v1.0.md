# 大纲生成—检查—修复闭环设计（v1.0）

> 目标：让「章纲展开」从**一次性生成 + 事后可选质检**，升级为**生成期防错 → 分层检查 → 定向修复 → 复验收敛**的单一闭环。
> 核心主张：**一个评分器，三个消费者，一个收敛循环**。检查不是终点，而是同时驱动「生成」和「修复」的信号源。

---

## 0. 现状盘点（基于代码实读，非推测）

### 0.1 生成侧 `gen_vol_chapter_plans`（steps/vol_chapter_plans.py）

- 富上下文、分批（默认 30 章/批），prompt 已注入 Tier1-5 + 卷间承接 + SEQ-01 章际衔接提示。
- **唯一的自动重试**是 `GEN-01`：JSON 解析失败 或 批次章数漂移。**没有任何质量层面的自纠**。
- 生成完调用 `finalize_volume_chapter_commit`：跑确定性 linter，命中 4 条阻断规则（CH-04 / CH-08 / SEQ-07 / CM-03）就 `linter_blocked=True`、暂存草稿、**不落库**，然后直接返回给用户。
- **结论**：生成是「单发 + 结构门禁」。一卷「字段全填满但语义空洞」的章纲会静默通过门禁落库。

### 0.2 检查侧——两套系统，互不汇合

| 系统 | 位置 | 触发 | 能力 |
|---|---|---|---|
| 确定性 linter（CH/SEQ/VB/RP/CM） | `outline_linter/` | **生成回路内自动**，阻断 commit | 形态检查：空字段、长度、2 字重叠、Jaccard 去重、节拍覆盖 |
| LLM 语义质检 `outline_quality_check` | `outline_ai_quality.py` + `qa_internal.py` | **独立手动**（`/ai-quality-check`） | 9-10 维：定位漂移/因果/跨卷/伏笔/人物弧/节奏/立意/故事圣经/篇幅 |

> `routes_quality.py` 注释明确写了两者「与全量生成拆开，避免生成失败和质检失败互相污染」。这个解耦是有意的，但代价是：**最强的语义质检，在生成那一刻从不参与**——除非用户事后记得点质检。

`outline_checks.py` 已把整体质检拆成 `causality / character_arc / foreshadow_audit` 三个**便宜的专项检查**（低温度、可并行），但同样未接入生成回路。

### 0.3 修复侧——两条路径，验证深度不对称

| 路径 | 复验方式 | 缺口 |
|---|---|---|
| `linter_fix_service`（勾选 linter 问题，轻量内联） | 仅 **re-lint** | 补丁过了形态检查就解除 `linter_blocked`；"付出了代价" 这种空洞补丁照样过 |
| `_run_outline_repair_workflow`（图编排） | apply 后 **re-QA**（图尾挂质检节点） | 不重跑 linter；**无收敛循环**（不会"还不行就再修一轮"）；不 gating |

---

## 1. 三个根因（要闭环必须同时解决）

- **R1 生成无质量自纠**：问题在生成那刻就已产生，却要等到事后检查才暴露。"减少问题"的最大杠杆在生成期，目前完全没用。
- **R2 检查与生成解耦**：确定性 linter 太弱（够不到"质"），强语义质检又不在场。两者从不在同一遍里互补。
- **R3 修复不收敛、不复利**：两条路径各验一半，没有"修复→复验→再修"的收敛保证；修复结果也不回流去改进下一次生成。

---

## 2. 目标架构：一个评分器，三个消费者，一个收敛循环

```
                    ┌─────────────────────────────────────────┐
                    │            LayeredGrader（统一评分器）       │
                    │  Tier-0 linter（确定性，免费，always）        │
                    │  Tier-1 causality QA（便宜语义，always）       │
                    │  Tier-2 holistic QA（昂贵 9 维，按需/高价值卷） │
                    │        ↓ 归一化输出 IssueSet（单一结构）        │
                    └─────────────────────────────────────────┘
                          │              │               │
              ┌───────────┘              │               └────────────┐
              ▼                          ▼                            ▼
     ① Generator（防错）          ② Gate（决策）                ③ Repairer（修复）
   把历史高频问题 + 维度          commit / block / repair       按 issue 定向打补丁
   要点回灌进 prompt              （区分高价值卷投入）            ↑__________________|
                                                              收敛循环：repair→regrade→repair
```

三个原则：
1. **检查统一成一个分层评分器**，输出**单一归一化 IssueSet**（复用现有 `linter_quality.linter_report_to_quality_report` + `_merge_outline_quality_reports`，把它提升为正式契约）。
2. **三个消费者吃同一份 IssueSet**：生成器、门禁、修复器。issue 的 `rule_id / type / field / chapter_number / severity` 是三方共享的"词汇表"。
3. **一个收敛循环**贯穿生成与修复：`generate → grade → (block→repair) → regrade`，带预算上限，保证终止。

---

## 3. 统一数据契约：`IssueSet` 与 `Directive`

落地前先锁定两个结构，三方都依赖它，避免再次出现"linter 报告"和"质检报告"两套格式互相翻译的熵增。

```python
# services/outline_quality/contract.py（新建，<150 行）
@dataclass
class Issue:
    rule_id: str          # "CH-04" / "QA-causality" / "QA-positioning_drift"
    tier: int             # 0=linter 1=causality 2=holistic
    severity: str         # critical/high/medium/low
    chapter_number: int | None
    field: str            # 归一化到 repair 字段名（沿用 repair_hints._FIELD_TO_REPAIR）
    message: str
    suggestion: str
    fixable: bool         # 是否可字段级自动修复
    fingerprint: str      # = hash(rule_id + chapter + field)，用于"同一问题反复出现"判定

@dataclass
class IssueSet:
    issues: list[Issue]
    dim_scores: dict[str, int]   # {causality:72, pacing:88, ...} 多维，不再 min 一刀切
    blocking: list[str]          # 命中的阻断 rule_id
    def must_fix_chapters(self) -> list[int]: ...
```

`Directive`（喂给生成器/修复器的"怎么改"）：

```python
@dataclass
class Directive:
    chapter_number: int
    target_fields: list[str]      # 优先改哪些字段（沿用 linter_fix_service._RULE_TARGET_FIELDS）
    hard_constraints: list[str]   # "choice_cost 必须含具体名词，禁止'代价/损失'裸词"
    good_bad_examples: list[tuple[str, str]]  # (反例, 正例)，针对高频空洞字段
```

---

## 4. 三大支柱

### 支柱一：生成期防错（最大的"减少问题"杠杆）—— R1

目标：让问题在生成那一刻就少产生，而不是事后修。三个具体手段，按性价比排序：

**4.1 项目级"高频问题回灌"块（必做，零额外 LLM 成本）**
当前每次生成都从零开始。改为：在 `build_vol_expand_ctx` 里查本项目历史 IssueSet（已存在 `volume.extra.outline_quality` / `linter_issues` / 新增 `rag_retrieval_log` 式的 issue 落库），统计 **Top-N 高频 fingerprint**，拼成一个注入块：

```
【本项目高频问题（前几卷反复出现，本卷重点规避）】
- choice_cost 空洞（出现 7 次）：必须写具体损失，禁止"付出代价/受了伤"裸词
- end_hook 套话（出现 5 次）：禁止"悬念丛生/接下来更精彩"，须含具体信息差
```
这把"检查发现的规律"反向变成"生成约束"，是 R2+R3 一并解决的关键回路（detect → prevent）。

**4.2 高频空洞字段的 good/bad few-shot（必做）**
`choice_cost` / `end_hook` / `villain_action` 是 linter 只能量长度、质检事后才抓的"重灾区"。在 prompt 里对这三个字段各给 1 组**正反例**（来自 `Directive.good_bad_examples`）。few-shot 对"填满但空洞"的抑制效果远好于纯指令。

**4.3 生成内自检 pass（可选，给高价值卷）**
对 `phase ∈ {climax, dark_hour}` 的卷或卷末高潮批，生成 JSON 后**在同一服务里追加一次廉价 `outline_check_causality`**（已有，低温度），把 broken_pairs 作为"二次生成提示"让模型自修一轮再返回。等于把 Tier-1 检查塞进生成回路，但只对值得的卷付费（呼应"分场关键点"——差异化投入）。

> 落点：`gen_vol_chapter_plans` 保持薄壳；4.1/4.2 在 `context_vol_expand.py` 与 `prompts/vol_chapter_plans_prompt.py` 增块；4.3 新增 `steps/vol_plan_self_check.py`（<150 行）由薄壳按 phase 条件调用。**不得**把逻辑堆回 643 行的 `vol_chapter_plans.py`（已超硬上限，登记册在册）。

### 支柱二：分层检查（让 linter 与 QA 在同一遍互补）—— R2

把 §0.2 的两套系统收编进 `LayeredGrader.grade(volume, tiers=...)`：

- **Tier-0 linter**：always。免费、确定性，挡死空字段/越界/批次断裂。
- **Tier-1 causality**：always（便宜）。验 linter 用 2 字重叠近似的因果链，**升级为 embedding 余弦**（复用 `outline_linter/embedding_seq.py` + pgvector），2 字重叠降级兜底。
- **Tier-2 holistic**：按需。完整 9 维 `outline_quality_check`。默认只对**高价值卷**（climax / dark_hour / 卷末高潮所在卷）或用户显式请求时跑。

关键改动：
- `grade()` 产出 §3 的 `IssueSet`，`dim_scores` 多维保留，废弃"overall = min(severity)"的单点塌缩（现 `linter_report_to_quality_report` 的 min 逻辑）。
- **Tier-0 已确定性命中的问题，不再交给 Tier-2 LLM 重找**（用 fingerprint 去重），省下的 token 让 Tier-2 在语义维度查得更深——正面回应 `outline_checks.py` 注释里的"每维深度不足"。
- 生成门禁从"只 Tier-0"升级为"Tier-0 + Tier-1"：因果断裂这种 Tier-1 问题也能在生成回路里被捕获，触发修复而非静默落库。

> 落点：新建 `services/outline_quality/grader.py`（编排薄壳，<150 行），调用既有 linter run + outline_checks + outline_ai_quality，**不重写**它们。`gate.py` 的 `finalize_volume_chapter_commit` 改为调用 grader 而非直接 `run_volume_linter`。

### 支柱三：定向修复 + 收敛复验（让"修得更好"可保证）—— R3

统一两条修复路径为一个**收敛循环**：

```python
# services/outline_quality/repair_loop.py（新建，<150 行）
async def repair_until_clean(volume, max_rounds=3, tiers=(0,1)):
    for round in range(max_rounds):
        issue_set = grader.grade(volume, tiers)
        if not issue_set.blocking and issue_set.ok():
            return Success(rounds=round)
        directives = build_directives(issue_set)          # issue → Directive
        patches = await outline_repair_plan(directives)   # 复用现有，按 directive 聚焦
        apply(patches, touched_chapters)
        # 关键：只对被改章节 + 邻章 regrade（便宜），不整卷重跑
        regrade = grader.grade(volume, tiers, only=touched±1)
        stuck = same_fingerprint_still_firing(issue_set, regrade)
        if stuck:
            escalate(stuck)         # 同一 fingerprint 修后仍在 → 升级指令再试一轮；
                                    # 第 max_rounds 轮仍 stuck → 标记 needs_human，停
    return Partial(stuck=stuck)
```

四个要点：
1. **收敛保证**：`max_rounds` 上限 + "stuck 检测"（同一 fingerprint 修复后仍触发 → 不无限循环，转人工）。
2. **双向复验**：修复后同时跑 Tier-0 + Tier-1（修 linter 的不能引入因果断裂，反之亦然）。这填上 `linter_fix_service` 只 re-lint、QA-repair 只 re-QA 的缺口。
3. **空洞补丁拦截**：`repair_loop` 通过 regrade 发现"补丁没真正消化 issue"（如 CH-04 修后 choice_cost 仍判空洞），自动升级修复指令（注入更强 hard_constraints + few-shot）再试，而非默默接受。
4. **只 regrade 受影响章节**：成本与 round 数线性而非平方。

> 落点：`repair_loop.py` 编排薄壳；复用 `outline_repair_plan`（已支持 linter_context / positioning / 生死 / 境界约束，不动）、`linter_fix_service.apply_linter_fix_patches`（落库）、`relint_volume_and_update_block_state`（改造为调用 grader 的双层复验）。两条旧路径成为新循环的两个特例入口。

---

## 5. 端到端闭环时序（展开一卷）

```
expand-chapters
  └─ gen_vol_chapter_plans
       ├─ [支柱一] ctx 注入：高频问题回灌 + good/bad few-shot
       ├─ 生成 JSON（批次）
       ├─ [支柱一 可选] climax/dark_hour 卷：causality 自检 + 自修一轮
       └─ finalize → [支柱二] grader.grade(Tier0+Tier1)
              ├─ 通过 → 落库，写 IssueSet 到 volume.extra（供下次回灌）
              └─ 阻断 → [支柱三] repair_until_clean(max_rounds=3)
                          ├─ 收敛 → 落库 + 写 IssueSet
                          └─ stuck → 暂存草稿 + needs_human + 精确指出哪章哪字段卡住
（用户可随时手动触发 Tier-2 全量质检 + 全书级质检，结果同样回灌）
```

---

## 6. 数据模型增量

- **`OutlineIssueLog`（新表，建议）**：每次 grade 落库 issue（project_id / volume_id / chapter_number / rule_id / tier / severity / fingerprint / round / status / created_at）。用途：(a) 支柱一的高频回灌；(b) 修复成功率/stuck 率可观测；(c) 阈值标定的数据基础。对齐仓库既有 `RagRetrievalLog` 的"过程落库"范式（CLAUDE.md 持久化硬规则）。
- `volume.extra` 增 `quality_state`：`{round, last_graded_at, dim_scores, needs_human, stuck_fingerprints}`，替代当前散落的 `linter_blocked / linter_issues / outline_quality`。

---

## 7. 分阶段落地（每阶段独立可上线、可回滚）

**Phase 1（最高 ROI，纯增量、零模型改造）**
- §3 契约 `contract.py` + §6 `OutlineIssueLog`。
- 支柱一 §4.1 高频回灌 + §4.2 few-shot。
- 预期：在不改检查/修复的前提下，先把"生成期问题率"压下来。可 A/B：开关 `ctx["enable_issue_feedback"]` 对比卷间 linter 命中数。

**Phase 2（统一检查）**
- `grader.py` 收编两套检查，输出 IssueSet；`gate.py` 改调 grader（Tier0+Tier1）。
- Tier-1 causality 的 embedding 化（SEQ-01/SEQ-07/VB-03 同步换 embedding）。

**Phase 3（收敛修复）**
- `repair_loop.py` + stuck 检测 + 双向复验；两条旧修复路径收口为入口。

**Phase 4（差异化投入 + 标定）**
- 支柱一 §4.3 生成内自检（仅高价值卷）。
- 用 Phase 1 起积累的 `OutlineIssueLog`，标定 linter 阈值（8/10/12 字、0.88 Jaccard、0.4 情感占比）与"哪条规则漏检率最高、该升级成语义检查"。

---

## 8. 风险与权衡

- **成本**：Tier-2 与 repair_loop 都调 LLM。缓解：Tier-2 默认按 phase 触发；repair 只 regrade 受影响章；`max_rounds` 封顶。
- **过度门禁误杀**：`gate.py` 现有"只阻断结构硬错误"的克制是对的。新增的 Tier-1 阻断**只加 causality 断裂一条**，其余语义问题走 advisory（修复但不阻断 commit）。
- **回灌偏置**：高频问题回灌可能让模型过度规避某类写法。缓解：回灌块只列 Top-N 且带"规避"而非"禁止全部"措辞，并随卷数衰减权重。
- **红线合规**：所有新逻辑落新建薄壳模块（grader / repair_loop / self_check / contract，各 <150 行），**不碰**登记册在册的超限文件（`vol_chapter_plans.py` 643 / `qa_internal.py` 878 / `context_assembler.py` 601）。给这些文件新增功能前需先按蓝图拆分。

---

## 9. 一句话总结

把「检查」从终点改造成**枢纽**：同一份 IssueSet 向前喂生成（防错）、居中做门禁、向后驱动修复，并用一个带预算和 stuck 检测的收敛循环把三者闭合。减少问题靠**生成期回灌 + few-shot**，修得更好靠**双向复验 + 收敛循环 + 空洞补丁拦截**。
