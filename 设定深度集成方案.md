# 设定→正文深度集成方案

> 整合三轮讨论成果：Bootstrap 设定如何真正流入写章流程
> 
> 版本：v1.0 · 2026-05-23

---

## 一、问题定位

### 根本病因

Bootstrap 生成了丰富的设定（势力、技能、法宝、故事线、伏笔、反派行动线……），但这些数据在写章时是**被动词典**——只有 RAG 碰巧命中才会用到，大量设定永远沉底。

### 现有写作流的断层

```
Bootstrap
  ├── PowerSystem / Faction / Character / Skill / Item
  ├── StoryLine / Foreshadow / ReaderPromise
  └── villain_arc / emotion_arc / core_mysteries

        ↕  断层：设定与写作流之间没有主动调度层

写章流程
  ├── 写前预警（展示给人看，AI 不感知）
  └── 生成分场（不知道预警说了什么）
```

写前预警和分场生成是**平行关系**，用户需要自己把预警内容"翻译"给分场 AI，而 AI 照样可能忽略。

---

## 二、核心设计思想

### 引入「本章投料清单」（ChapterIngredients）

在 Bootstrap 设定和写作流之间，插入一个**主动调度层**：

```
Bootstrap 设定库（所有表）
        ↓ 主动拉取、计算欠账
ChapterIngredients（结构化投料清单）
        ↓ 作为强制约束输入
生成分场（把约束分配到各场景）
        ↓ 每场景携带自己的约束子集
场景起草（带约束写正文）
        ↓ 写完后验收
写后核验（哪些约束被满足，更新账本）
```

**关键原则**：

- 投料清单是**给 AI 看的约束参数**，不只是给人看的提示
- 场景从规划时就携带约束，起草时直接读，无需重新查询
- 写后验收闭合债务追踪，形成完整闭环

---

## 三、完整数据流

```
① 打开章节 / 点击「重新检测」
   ─→ ChapterIngredients 服务计算（查询 8 张表）
   ─→ 结构化 JSON 存入 OutlineNode.extra.pre_write_constraints
   ─→ 写前预警面板：人可读版本 + 结构化数据同时输出

② 点击「生成分场」
   ─→ 读取 pre_write_constraints（已计算好，无需再查询）
   ─→ 分场规划 prompt 注入完整约束块
   ─→ AI 将约束分配到 4-8 个场景
   ─→ 每个 Scene 写入专属约束字段
      · storyline_moves    故事线推进指令
      · faction_color      势力/地盘着色
      · asset_spotlight    技能/法宝聚光灯
      · foreshadow_ops     伏笔操作（埋/推进/回收）
      · debt_flags         本场景应还的债务
      · structural_warnings 结构预警（字数/节奏）

③ 逐场起草（draft/stream）
   ─→ prompt 直接读 scene 自身约束字段，无需重查
   ─→ AI 带着精确约束写正文

④ 拼合完成（stitch）+ 写后核验
   ─→ 检查各场景约束是否被满足
   ─→ 回填 scene.checklist_result
   ─→ 更新 Project.extra.payoff_ledger（爽点账本）
   ─→ 未满足的关键约束 → 写入 QualityDebt
   ─→ 已满足的 ReaderPromise → 标记 fulfilled
```

---

## 四、数据模型变更

### 4.1 Scene 模型新增字段

```python
# apps/backend/app/models/scene.py

class Scene(Base):
    # ── 现有字段（不变）────────────────────────────
    id: UUID
    project_id: UUID
    chapter_id: UUID | None
    outline_node_id: UUID | None
    order: int
    pov_character_id: UUID | None
    characters_on_stage: list[UUID]       # JSON
    goal: str
    conflict: str
    turn: str
    hook: str
    hook_strength: int
    word_budget: int
    pacing: str
    sensory_focus: str
    status: str                           # planned/written/reviewed
    content: str | None
    location_name: str | None

    # ── 新增：规划时写入的约束区 ────────────────────
    
    # 本场景必须推进的故事线
    # [{"storyline_id": "xx", "name": "感情线",
    #   "from_state": "互相试探", "to_state": "男主首次出手护她被误解"}]
    storyline_moves: list[dict] | None    # JSON, nullable

    # 场景所在地的势力着色
    # {"faction_id": "xx", "name": "天机阁",
    #  "atmosphere": "监视/压迫", "npc_default_attitude": "戒备"}
    faction_color: dict | None            # JSON, nullable

    # 本场景技能/法宝聚光灯（仅涉及角色的相关项）
    # [{"type": "skill", "id": "xx", "name": "追魂丝",
    #   "visual": "指尖银丝", "cost": "灵力大量消耗", "weakness": "范围小"}]
    asset_spotlight: list[dict] | None    # JSON, nullable

    # 伏笔操作指令
    # [{"foreshadow_id": "xx", "name": "锈蚀斩刀",
    #   "op": "hint",  # lay / hint / resolve
    #   "suggested_method": "让NPC无意提及"}]
    foreshadow_ops: list[dict] | None     # JSON, nullable

    # 本场景应偿还的债务
    # [{"type": "payoff", "overdue_chapters": 3, "setup_desc": "陆师兄当众羞辱"},
    #  {"type": "storyline_gap", "storyline": "感情线", "gap_chapters": 8}]
    debt_flags: list[dict] | None         # JSON, nullable

    # 结构预警（规划时生成）
    # [{"code": "CROWD", "level": "warn",
    #   "msg": "5角色/1500字，浅描风险，建议聚焦2-3人"}]
    structural_warnings: list[dict] | None # JSON, nullable

    # ── 新增：起草后回填 ────────────────────────────

    # 约束满足情况（stitch 后核验写入）
    # {"storyline_moves": [true, false], "foreshadow_ops": [true],
    #  "debt_cleared": true, "score": 0.75}
    checklist_result: dict | None         # JSON, nullable
```

**Migration**：新增 7 个 nullable JSON 列，向后兼容。

### 4.2 OutlineNode.extra 新增键（无 schema 变更）

```json
{
  "pre_write_constraints": {
    "computed_at": "2026-05-23T10:00:00",
    "model_used": "gemini-xxx",
    "character_lock": {},
    "forbidden_items": [],
    "storyline_moves": [],
    "faction_colors": [],
    "asset_spotlight": [],
    "foreshadow_ops": [],
    "debt_flags": [],
    "villain_offscreen": "",
    "emotional_quota": {},
    "relationship_snapshots": [],
    "world_rules": []
  }
}
```

### 4.3 Project.extra 新增键（爽点账本）

```json
{
  "payoff_ledger": {
    "last_payoff_chapter": 12,
    "payoff_debt": 1,
    "pending_setups": [
      {"desc": "陆师兄当众羞辱", "laid_chapter": 8},
      {"desc": "城主府通缉令", "laid_chapter": 10}
    ]
  }
}
```

---

## 五、新建服务：ChapterIngredients

### 5.1 文件位置

```
apps/backend/app/services/ai/
├── chapter_ingredients.py          # 主服务（计算逻辑）
└── prompts/
    └── chapter_ingredients.py      # prompt 模板（如需 LLM 辅助计算）
```

### 5.2 核心计算逻辑（无需 LLM，纯 DB 查询）

```python
# services/ai/chapter_ingredients.py

async def compute_chapter_ingredients(
    db: AsyncSession,
    project_id: UUID,
    outline_node_id: UUID,
) -> ChapterIngredients:
    """
    主动从所有设定库拉取与本章相关的约束信息。
    
    纯数据库计算，不调用 LLM，速度快（< 500ms）。
    结果持久化到 OutlineNode.extra.pre_write_constraints。
    """
    node = await _get_outline_node(db, outline_node_id)
    project = await _get_project(db, project_id)

    return ChapterIngredients(
        character_lock   = await _build_character_lock(db, node),
        forbidden_items  = await _extract_forbidden(db, node, project),
        storyline_moves  = await _compute_storyline_moves(db, node, project),
        faction_colors   = await _compute_faction_colors(db, node, project),
        asset_spotlight  = await _compute_asset_spotlight(db, node),
        foreshadow_ops   = await _compute_foreshadow_ops(db, node, project),
        debt_flags       = await _compute_debt_flags(db, node, project),
        villain_offscreen= _extract_villain_offscreen(project, node),
        emotional_quota  = _extract_emotional_quota(project, node),
        relationship_snapshots = await _compute_relationships(db, node),
        world_rules      = await _extract_world_rules(db, node, project),
    )
```

### 5.3 各计算子函数逻辑

**`_compute_storyline_moves`**
```
查询 node.storyline_ids 关联的 StoryLine
→ 对每条线：查询本线最近一次出现是哪章（扫 OutlineNode.storyline_ids）
→ 断档 > 阈值（可配置，默认 5 章）→ 标记为 MUST_ADVANCE
→ 结合 node 的情节描述，生成「from_state → to_state」推进指令
```

**`_compute_faction_colors`**
```
查询 node.involved_character_ids 的角色当前 faction_id
→ 查询场景地点（location_name）匹配的 Faction
→ 拉取 Faction.description / extra 中的文化/氛围字段
→ 生成着色卡
```

**`_compute_asset_spotlight`**
```
查询 node.key_skill_ids → Skill 表：name / description / visual_effect / cost / weakness
查询 node.key_item_ids  → Item  表：name / appearance / spiritual_reaction / lore
→ 过滤：只保留在场角色拥有/可使用的
```

**`_compute_foreshadow_ops`**
```
查询 Foreshadow 表 status=open，按 laid_chapter 排序
→ 超过 min_return_window 且未 resolved → op=hint 或 op=resolve
→ node.foreshadows_laid  → op=lay（新埋）
→ node.foreshadows_resolved → op=resolve（本章回收）
```

**`_compute_debt_flags`**
```
payoff_debt：
  读 project.extra.payoff_ledger.payoff_debt
  欠 ≥ 1 且 pending_setups 非空 → 生成 payoff flag

storyline_gap：
  同 _compute_storyline_moves，断档超阈值的线 → 生成 gap flag

promise_due：
  查询 ReaderPromise status=open，expected_chapter_window 包含当前章 → 生成 promise flag

emotion_debt：
  读 project.extra.emotion_arc 对应卷的情绪收支
  连续正向章节过多 → 生成 tension_needed flag
```

**`_compute_relationships`**
```
node.involved_character_ids 两两组合
→ 查询 CharacterRelationship 表
→ 提取：trust_level / hidden_secret / last_interaction / unresolved_tension
→ 生成关系快照（仅在场角色两两之间）
```

---

## 六、改造：写前预警

### 6.1 现状 vs 目标

| 维度 | 现在 | 目标 |
|------|------|------|
| 输出格式 | 展示文字 | 展示文字 + 结构化 JSON |
| 读者 | 人 | 人 + 分场生成 AI |
| 计算方式 | LLM 生成 | **DB 查询为主**，LLM 只负责生成「写法简报」|
| 结果存储 | 不持久化 | 存入 OutlineNode.extra.pre_write_constraints |

### 6.2 新的处理拆分

```
写前预警 = 两步

Step A（DB 计算，< 500ms，无 LLM）：
  → compute_chapter_ingredients()
  → 持久化到 OutlineNode.extra.pre_write_constraints
  → 返回结构化 JSON

Step B（LLM 生成，流式，现有逻辑保留）：
  → 以 Step A 的结构化数据为输入
  → 生成「本章写法简报」（开篇策略、冲突爆点……）
  → SSE 流式推送给前端
```

### 6.3 API 变更

```
POST /api/v1/projects/{pid}/ai/chapter-ingredients
  Body: { outline_node_id, chapter_id? }
  
  Response:
  {
    "structured": { ...ChapterIngredients JSON },
    "display": { ...现有写前预警展示字段 }
  }
```

（可复用现有写前预警端点，在 response 中增加 `structured` 字段）

---

## 七、改造：场景规划（plan-save）

### 7.1 prompt 注入新增约束块

`scene_routes.py` 的分场规划 prompt，在现有「章节背景」之后注入：

```
## 本章约束（必须分配到场景中）

### 故事线推进任务
{% for move in constraints.storyline_moves %}
- [{{ move.name }}] {{ move.from_state }} → {{ move.to_state }}（MUST）
{% endfor %}

### 债务偿还
{% for debt in constraints.debt_flags %}
- [{{ debt.type }}] {{ debt.desc }}（已欠 {{ debt.overdue }} 章，MUST）
{% endfor %}

### 伏笔操作
{% for op in constraints.foreshadow_ops %}
- {{ op.op | upper }}「{{ op.name }}」：{{ op.suggested_method }}
{% endfor %}

### 势力地盘着色（选择最相关 1 个场景注入）
{% for fc in constraints.faction_colors %}
- 「{{ fc.name }}」势力：{{ fc.atmosphere }}，NPC 默认态度：{{ fc.npc_default_attitude }}
{% endfor %}

### 反派幕后动态（可作为 NPC 台词/环境线索隐约呈现）
{{ constraints.villain_offscreen }}

### 情绪预算
本章情绪上限：{{ constraints.emotional_quota.budget }}
当前卷基调：{{ constraints.emotional_quota.volume_tone }}
禁止：{{ constraints.emotional_quota.forbidden_tone }}

---
请将以上约束合理分配到 4-8 个场景中。
每个场景输出时，在 extra 字段标注：本场景承接了哪些约束。
```

### 7.2 场景生成结果写入新字段

分场生成完成后，解析 AI 输出的约束分配，写入各 Scene 的新字段：

```python
# 伪代码
for scene_data in ai_output["scenes"]:
    scene = Scene(
        goal=scene_data["goal"],
        ...
        storyline_moves  = scene_data.get("storyline_moves"),
        faction_color    = scene_data.get("faction_color"),
        asset_spotlight  = scene_data.get("asset_spotlight"),
        foreshadow_ops   = scene_data.get("foreshadow_ops"),
        debt_flags       = scene_data.get("debt_flags"),
        structural_warnings = _compute_structural_warnings(scene_data),
    )
```

---

## 八、改造：场景起草（draft/stream）

### 8.1 draft prompt 直接读 scene 约束字段

不需要重新查询数据库，场景起草时直接用 scene 自身携带的约束：

```python
# 新增工具函数：build_scene_constraint_block(scene: Scene) -> str
def build_scene_constraint_block(scene: Scene) -> str:
    parts = []
    
    if scene.faction_color:
        fc = scene.faction_color
        parts.append(
            f"## 地盘氛围\n"
            f"此地为「{fc['name']}」势力管辖。\n"
            f"氛围：{fc['atmosphere']}\n"
            f"周围 NPC 默认态度：{fc['npc_default_attitude']}"
        )
    
    if scene.asset_spotlight:
        cards = "\n".join(
            f"- 【{a['name']}】{a['visual']}（代价：{a['cost']}，弱点：{a['weakness']}）"
            for a in scene.asset_spotlight
        )
        parts.append(f"## 本场可用技能/法宝\n{cards}")
    
    if scene.storyline_moves:
        moves = "\n".join(
            f"- {m['name']}：{m['from_state']} → {m['to_state']}"
            for m in scene.storyline_moves
        )
        parts.append(f"## 必须推进的故事线\n{moves}")
    
    if scene.foreshadow_ops:
        ops = "\n".join(
            f"- {op['op'].upper()}「{op['name']}」：{op['suggested_method']}"
            for op in scene.foreshadow_ops
        )
        parts.append(f"## 伏笔操作\n{ops}")
    
    if scene.debt_flags:
        debts = "\n".join(f"- {d['type']}：{d['desc']}" for d in scene.debt_flags)
        parts.append(f"## 本场必须偿还的债务\n{debts}")
    
    if scene.relationship_snapshots:  # 从 pre_write_constraints 读在场角色关系
        ...
    
    return "\n\n".join(parts)
```

### 8.2 关系快照注入

起草时补充人物关系（从 OutlineNode.extra.pre_write_constraints 读取，
按 scene.characters_on_stage 过滤）：

```
## 本场在场角色关系
陆鸣 ↔ 苏晴：信任值 3/10，苏晴不知道陆鸣碎骨境真相，
        上次互动结局：陆鸣沉默拒绝了她的帮助（ch.11）
        未解张力：苏晴以为陆鸣在故意疏远
```

---

## 九、新增：写后核验

### 9.1 触发时机

`stitch` 接口拼合完成后，异步触发核验（不阻塞返回）。

### 9.2 核验逻辑

```python
# services/ai/scene_checklist.py

async def verify_scene_constraints(
    db: AsyncSession,
    scene_id: UUID,
) -> ChecklistResult:
    """
    核验场景正文是否满足规划时写入的约束。
    轻量 LLM 调用（temperature=0.1，prompt 简短）。
    """
    scene = await db.get(Scene, scene_id)
    if not scene.content or not scene.debt_flags:
        return None
    
    # 构造核验 prompt
    prompt = f"""
    场景正文（节选）：{scene.content[:800]}
    
    请判断以下约束是否在正文中得到体现（JSON 回答）：
    1. 故事线推进：{scene.storyline_moves}
    2. 伏笔操作：{scene.foreshadow_ops}
    3. 债务偿还：{scene.debt_flags}
    
    输出格式：
    {{"storyline_ok": [true/false, ...], "foreshadow_ok": [true/false, ...],
      "debt_cleared": true/false, "score": 0.0-1.0, "notes": "..."}}
    """
    
    result = await _call_ai(prompt, task="quality.check")
    
    # 回填
    scene.checklist_result = result
    
    # 更新债务账本
    if result["debt_cleared"]:
        await _clear_payoff_debt(db, scene.project_id)
    else:
        await _increment_payoff_debt(db, scene.project_id)
    
    await db.commit()
    return result
```

---

## 十、前端改造

### 10.1 写前预警面板（增强）

现有面板保留展示逻辑，新增：
- 「债务看板」区块：红/黄/绿三色标注（爽点债、断档线、承诺到期）
- 「约束摘要」折叠区：给有经验用户查看完整 JSON

```
┌─ 写前预警 ────────────────────────────┐
│ ⚠ 2 条风险 · gemini         重新检测  │
├───────────────────────────────────────┤
│ ✅ 未发现高危风险，可以动笔            │
│                                       │
│ 🔴 债务看板                           │
│    · 爽点欠账 +3章（上次：ch.12）     │
│    · 感情线断档 8章                   │
│    · 读者承诺「ch.15内决战」临近到期  │
│                                       │
│ 📌 主角状态锁（现有，不变）            │
│ 🚫 本章禁止（现有，不变）             │
│ ✏ 写法简报（现有，不变）              │
└───────────────────────────────────────┘
```

### 10.2 分场写作面板（增强）

场景卡片展示约束标签：

```
┌─ 场景 #3 ─────────────────────────────────────┐
│ POV: 陆鸣  · 预算 1800字  · 节奏: 紧凑        │
│                                                │
│ 目标：突破封锁，带苏晴离开天机阁外围            │
│ 冲突：追魂丝灵力耗尽，正面对抗无法持续          │
│ 转折：锈蚀斩刀意外引发残留煞气，震退追兵        │
│ 钩子：斩刀碎裂，但煞气渗入丹田，留下隐患        │
│                                                │
│ ─── 约束标签 ──────────────────────────────── │
│ [还债·爽点] [感情线→误解加深] [HINT·锈蚀斩刀] │
│ [天机阁地盘·压迫氛围]  ⚠ 人数/字数偏紧        │
│                                                │
│              [生成正文]                        │
└────────────────────────────────────────────────┘
```

写完后约束标签变为核验状态：

```
│ [✅还债·爽点] [✅感情线→误解] [❌HINT·锈蚀斩刀] │
│  ⚠ 伏笔未处理 → 已记入质检欠账                │
```

---

## 十一、文件改动清单

### 新建文件

| 文件 | 说明 |
|------|------|
| `apps/backend/app/services/ai/chapter_ingredients.py` | 投料清单主服务（约 180 行） |
| `apps/backend/app/services/ai/prompts/chapter_ingredients.py` | prompt 模板 |
| `apps/backend/app/services/ai/scene_checklist.py` | 写后核验服务（约 120 行） |
| `apps/backend/app/alembic/versions/xxxx_scene_constraint_fields.py` | Scene 新增 7 个 JSON 列 |

### 改动文件

| 文件 | 改动内容 | 预估增量 |
|------|----------|---------|
| `apps/backend/app/models/scene.py` | 新增 7 个 nullable JSON 列 | +30 行 |
| `apps/backend/app/routers/ai/scene_routes.py` | plan-save 注入约束块；stitch 后触发核验 | +80 行 |
| `apps/backend/app/services/ai/context_builder.py` | `build_scene_constraint_block()` | +60 行 ⚠️ 已超硬上限，需同步拆分 |
| `apps/backend/app/routers/ai/` | 写前预警端点返回 structured 字段 | +30 行 |
| `apps/client/src/components/Writing/ChapterEditor/ScenePipelinePanel/` | 债务看板 + 约束标签 + 核验状态 | 新增子组件 |

> ⚠️ `context_builder.py` 已在上帝文件登记册，新增代码必须同步拆分。
> 建议将 `build_scene_constraint_block` 放入新文件
> `services/ai/scene_constraint_builder.py`，从 context_builder 中引入。

---

## 十二、实施路径

按收益/成本比排序，建议分三批落地：

### 第一批（P0，1-2天，效果最显著）

1. **故事线推进指令注入**
   - 实现 `_compute_storyline_moves`
   - 分场 prompt 加故事线约束块
   - 效果：感情线/支线不再消失几十章

2. **技能/法宝聚光灯注入**
   - 实现 `_compute_asset_spotlight`
   - 起草 prompt 加 `asset_spotlight` 块
   - 效果：战斗/修炼场景立即有具体感

3. **写前预警返回 structured JSON**
   - 为后续所有注入奠定数据基础

### 第二批（P1，2-3天，世界观深度）

4. **势力地盘着色**
   - 实现 `_compute_faction_colors`
   - 效果：场景描写有政治/文化质感

5. **人物关系快照**
   - 实现 `_compute_relationships`
   - 效果：多人对话有话里有话的层次

6. **债务看板 UI**
   - 前端展示爽点欠账/断档标记
   - 效果：编辑视角可见，人工可干预

### 第三批（P2，3-4天，闭环完善）

7. **写后核验 + 债务账本**
   - 实现 `scene_checklist.py`
   - 更新 `payoff_ledger`
   - 效果：形成真正的债务追踪闭环

8. **反派幕后动态注入**
   - 从 `villain_arc` 提取当前卷条目
   - 效果：无处不在的无形压迫感

9. **情绪预算约束**
   - 防止正向/负向情绪连续过多
   - 效果：读者不会审美疲劳

---

## 十三、设计边界说明

**本方案不改变的事**：
- Bootstrap 步骤（设定生成逻辑不动）
- 章节/场景的核心写作流（goal/conflict/turn/hook 逻辑不变）
- 现有写前预警的展示内容（增量，不替换）
- LLM 调用次数（写前预警 + 分场各一次，不增加）

**本方案新增的 LLM 调用**：
- 写后核验（轻量，temperature=0.1，仅在 stitch 后异步触发）

**可配置项**（后续可暴露到管理后台）：
- 故事线断档阈值（默认 5 章）
- 爽点欠债阈值（默认 3 章，触发强制还债）
- 伏笔最长悬置章数（默认 15 章，超出必须 hint）
